[all:vars]
ansible_user=ubuntu #Write Instance's username
ansible_ssh_private_key_file=/path/to/your/private/key

[web-server]
web-01 ansible_host=1.1.1.1 # Write Web server's public IP

[db-server]
db-01 ansible_host=2.2.2.2 # Write DB server's public IP

# Note: Group names must match the hosts defined in the playbook.